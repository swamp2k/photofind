# Quality performance and global status

Implementation note only: Quality known-event membership is now built in one linear pass instead of querying every photo against every event. Long-running PhotoFind work reports through the global sidebar activity indicator.
